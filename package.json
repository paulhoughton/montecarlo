{
  "name": "montecarlo",
  "version": "0.0.1",
  "description": "Monte Carlo simulation running in a web worker",
  "scripts": {
    "build": "npm run compile && npm run bundle",
    "bundle": "rollup -f iife -n monte lib/es6_global/src/monte.js -o build/monte.js",
    "compile": "bsb"
  },
  "license": "MIT",
  "devDependencies": {
    "bs-platform": "^1.7.5",
    "rollup": "^0.43.0"
  }
}